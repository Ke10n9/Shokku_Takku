<li>
  <%= profile_image user %>
  <%= link_to user.name, user %>
  <%= render 'stats' if current_user?(user) %>
  <% if current_user.admin? && !current_user?(user) %>
  | <%= render partial: 'delete_user', locals: { user: user } %>
  <% end %>
</li>
