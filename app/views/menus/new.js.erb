<% provide(:modal_header, "新しい献立を登録しましょう。") %>

$("#user-modal").html("<%= escape_javascript(render 'shared/form') %>");
$("#user-modal").modal("show");

$('#menu_picture').bind('click', function() {
  $('#preview').addClass("non-active");
  $('#form-menu-img').removeClass("non-active");
  $('#message-with-picture').addClass("hidden");
});

$('#menu_picture').bind('change', function() {
  var size_in_megabytes = this.files[0].size/1024/1024;
  if (size_in_megabytes > 5) {
    alert('最大ファイルサイズは5MBです。');
  }

  $('#form-menu-img').addClass("non-active");
  $('#preview').removeClass("non-active");
  $('#message-with-picture').removeClass("hidden");
  var file = $(this).prop('files')[0];
  var reader = new FileReader();
  reader.onload = function(e) {
    $('#preview').attr('src', e.target.result);
  }
  reader.readAsDataURL(file);
});
