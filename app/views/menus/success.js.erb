$('input[type="text"]').val('');
$('#user-modal').modal('hide')
location.reload();

// window.addEventListener('load', function () {
//
//     // ページ表示完了した5秒後にリロード
//     setTimeout(doReloadNoCache, 5000);
//
// });
