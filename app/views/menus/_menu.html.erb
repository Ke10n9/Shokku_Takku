<li class="item">
  <div class="picture">
    <%= image_tag menu.picture.url if menu.picture? %>
  </div>
  <span class="info">
    <span class="datetime"><%= menu.date %>　<%= menu.time %></span>
    <ul class="dishes">
      <% menu.dishes.all.each do |dish| %>
        <li class="dish"><%= dish.name %></li>
      <% end %>
    </ul>
    <span class="user">
      <%= menu.user.name %>さんの投稿
    </span>
  </span>
</li>
